program NewTest;

uses
  Forms,
  URelay in 'URelay.pas' {FRelay},
  UNTest in 'UNTest.pas' {Form1},
  UKey in 'UKey.pas' {FKey},
  UOption in 'UOption.pas' {FOption},
  UMem in 'UMem.pas' {FMem},
  UCMem in 'UCMem.pas' {FCmem},
  URep in 'URep.pas' {FRep},
  UReg in 'UReg.pas' {FReg},
  ULan in 'ULan.pas' {FAlarm},
  UChannel in 'UChannel.pas' {FormChannel};
//  SDalComp in '..\SocketController\SDalComp.pas';

{$R NewTest.RES}

begin
  Application.Initialize;
 // Application.HelpFile := 'D:\DALNOV\DOC_TA53\Dp1.hlp';
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TFRelay, FRelay);
  Application.CreateForm(TFKey, FKey);
  Application.CreateForm(TFOption, FOption);
  Application.CreateForm(TFMem, FMem);
  Application.CreateForm(TFCmem, FCmem);
  Application.CreateForm(TFRep, FRep);
  Application.CreateForm(TFReg, FReg);
  Application.CreateForm(TFAlarm, FAlarm);
  Application.CreateForm(TFormChannel, FormChannel);
  Application.Run;
end.
